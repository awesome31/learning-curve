/**
 * Can we generate a tree from a single traversal? The answer is NO. We can only
 * generate unique trees using preorder/potsorder + inorder trees.
 */

#include "../Tree.h"
#include <iostream>
#include <stack>

// preorder = [3,9,20,15,7], inorder = [9,3,15,20,7]
TreeNode *buildTree(vector<int> &preorder, vector<int> &inorder) {}

int main() {}